//Use the find() method to return first element in a array that satisfies the provided testing function
const numbers = [3, 52, 1, 89, 2];
const found = numbers.find((element) => element > 10);

console.log(found);
